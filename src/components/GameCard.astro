---
import type { Game } from "@/constants/games"
import GamePreview from "./GamePreview.astro"

interface Props {
    game: Game
}

const { game } = Astro.props
const isPlayable = game.status === "playable"
---

<a
    href={game.path}
    class="game-card"
    style={`--preview-accent: ${game.accent}; --preview-soft: ${game.accentSoft}; --card-accent: ${game.accent};`}
    aria-label={`${isPlayable ? "Play" : "View progress for"} ${game.title}`}
>
    <div class="game-card__preview">
        <GamePreview gameId={game.id} />
        <span class={`game-card__status ${isPlayable ? "is-playable" : ""}`}>
            <i aria-hidden="true"></i>
            {isPlayable ? "Playable" : "In development"}
        </span>
    </div>

    <div class="game-card__body">
        <div class="game-card__heading">
            <h3>{game.title}</h3>
            <span>{game.category}</span>
        </div>
        <p>{game.description}</p>
        <div class="game-card__footer">
            <span class="game-card__time">{game.session}</span>
            <span class={`game-card__action ${!isPlayable ? "is-muted" : ""}`}>
                {isPlayable ? "Play" : "View progress"}
                <i aria-hidden="true">→</i>
            </span>
        </div>
    </div>
</a>

<style>
    .game-card {
        display: flex;
        height: 100%;
        overflow: hidden;
        flex-direction: column;
        border: 1px solid var(--arcade-border);
        border-radius: var(--radius-lg);
        background: var(--arcade-surface);
        box-shadow: 0 12px 30px rgba(0, 0, 0, 0.16);
        transition: border-color 180ms ease, box-shadow 180ms ease, transform 180ms ease;
    }

    .game-card:hover {
        border-color: color-mix(in srgb, var(--card-accent) 60%, var(--arcade-border));
        box-shadow: 0 18px 45px rgba(0, 0, 0, 0.3), 0 0 22px color-mix(in srgb, var(--card-accent) 9%, transparent);
        transform: translateY(-4px);
    }

    .game-card__preview {
        position: relative;
        min-height: 12.5rem;
        overflow: hidden;
        border-bottom: 1px solid var(--arcade-border);
    }

    .game-card__preview::after {
        position: absolute;
        right: 0;
        bottom: 0;
        left: 0;
        height: 2px;
        background: linear-gradient(90deg, transparent, var(--card-accent), transparent);
        content: "";
        opacity: 0.7;
    }

    .game-card__preview :global(.game-preview) {
        min-height: 12.5rem;
    }

    .game-card__status {
        position: absolute;
        top: 0.8rem;
        left: 0.8rem;
        display: inline-flex;
        align-items: center;
        gap: 0.4rem;
        padding: 0.35rem 0.55rem;
        border: 1px solid var(--arcade-border-bright);
        border-radius: 999px;
        color: var(--arcade-muted);
        background: rgba(13, 9, 18, 0.82);
        backdrop-filter: blur(8px);
        font-family: var(--font-utility);
        font-size: 0.56rem;
        font-weight: 600;
        letter-spacing: 0.04em;
        text-transform: uppercase;
    }

    .game-card__status i {
        width: 0.38rem;
        height: 0.38rem;
        border-radius: 50%;
        background: var(--arcade-yellow);
        box-shadow: 0 0 8px rgba(244, 196, 48, 0.45);
    }

    .game-card__status.is-playable i {
        background: var(--arcade-green);
        box-shadow: 0 0 8px rgba(130, 220, 61, 0.5);
    }

    .game-card__body {
        display: flex;
        flex: 1;
        flex-direction: column;
        padding: 1.25rem;
    }

    .game-card__heading {
        display: flex;
        align-items: baseline;
        justify-content: space-between;
        gap: 1rem;
        margin-bottom: 0.65rem;
    }

    h3 {
        margin-bottom: 0;
        color: #fff;
        font-size: 1.25rem;
        font-weight: 750;
        letter-spacing: -0.035em;
        line-height: 1.2;
    }

    .game-card__heading > span {
        flex: 0 0 auto;
        color: var(--arcade-subtle);
        font-family: var(--font-utility);
        font-size: 0.58rem;
        letter-spacing: 0.05em;
        text-transform: uppercase;
    }

    p {
        margin-bottom: 1.35rem;
        color: var(--arcade-muted);
        font-size: 0.86rem;
        line-height: 1.55;
    }

    .game-card__footer {
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 1rem;
        margin-top: auto;
        padding-top: 0.9rem;
        border-top: 1px solid var(--arcade-border);
    }

    .game-card__time {
        color: var(--arcade-subtle);
        font-family: var(--font-utility);
        font-size: 0.62rem;
        text-transform: uppercase;
    }

    .game-card__action {
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
        color: #fff;
        font-size: 0.78rem;
        font-weight: 750;
    }

    .game-card__action i {
        color: var(--card-accent);
        font-size: 1rem;
        font-style: normal;
        transition: transform 180ms ease;
    }

    .game-card:hover .game-card__action i {
        transform: translateX(3px);
    }

    .game-card__action.is-muted {
        color: var(--arcade-muted);
    }
</style>
