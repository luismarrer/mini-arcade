<section class="stack-lab" aria-labelledby="stack-status">
    <div class="stack-stage" aria-hidden="true">
        <div class="stack-stage__skyline"></div>
        <div class="stack-stage__target"></div>
        <div class="stack-tower">
            <span class="stack-block stack-block--1"></span>
            <span class="stack-block stack-block--2"></span>
            <span class="stack-block stack-block--3"></span>
            <span class="stack-block stack-block--4"></span>
            <span class="stack-block stack-block--5"></span>
            <span class="stack-block stack-block--moving"></span>
        </div>
        <span class="stack-score">HEIGHT 05</span>
    </div>

    <div class="stack-notes">
        <p class="stack-kicker">Timing prototype / 15%</p>
        <h2 id="stack-status">The tower is standing. The game loop is next.</h2>
        <p>
            I’m tuning the snap, crop and speed-up that make each block placement
            feel fair. This page keeps the visual idea alive while that interaction
            is still being built.
        </p>

        <dl>
            <div><dt>Input</dt><dd>One tap</dd></div>
            <div><dt>Goal</dt><dd>Build upward</dd></div>
            <div><dt>Next</dt><dd>Scoring + sound</dd></div>
        </dl>

        <a href="/all-games" class="button button--secondary button--small">Play a finished game</a>
    </div>
</section>

<style>
    .stack-lab {
        display: grid;
        grid-template-columns: minmax(17rem, 0.9fr) minmax(0, 1.1fr);
        overflow: hidden;
        border: 1px solid #4b3823;
        border-radius: 1rem;
        color: #f8fafc;
        background: #111827;
    }

    .stack-stage {
        position: relative;
        display: grid;
        min-height: 29rem;
        place-items: end center;
        overflow: hidden;
        border-right: 1px solid #4b3823;
        background:
            radial-gradient(circle at 50% 25%, rgba(245, 158, 11, 0.18), transparent 45%),
            linear-gradient(180deg, #161d35 0%, #0f172a 72%);
    }

    .stack-stage__skyline {
        position: absolute;
        inset: auto 0 0;
        height: 28%;
        opacity: 0.34;
        background: repeating-linear-gradient(90deg, #070b14 0 2.8rem, transparent 2.8rem 3.1rem);
        clip-path: polygon(0 45%, 8% 45%, 8% 17%, 16% 17%, 16% 60%, 28% 60%, 28% 30%, 38% 30%, 38% 67%, 51% 67%, 51% 8%, 64% 8%, 64% 52%, 76% 52%, 76% 24%, 86% 24%, 86% 61%, 100% 61%, 100% 100%, 0 100%);
    }

    .stack-stage__target {
        position: absolute;
        inset: 12% 12% auto;
        height: 1px;
        background: repeating-linear-gradient(90deg, rgba(245, 158, 11, 0.5) 0 0.55rem, transparent 0.55rem 1rem);
    }

    .stack-tower {
        position: relative;
        z-index: 1;
        display: flex;
        width: min(72%, 16rem);
        flex-direction: column-reverse;
        align-items: center;
        gap: 0.2rem;
        padding-bottom: 2rem;
    }

    .stack-block {
        width: 100%;
        height: 2.15rem;
        border: 1px solid rgba(255, 255, 255, 0.22);
        border-radius: 0.18rem;
        background: #ef4444;
        box-shadow: 0 8px 0 rgba(0, 0, 0, 0.18);
    }

    .stack-block--2 { width: 92%; background: #f97316; transform: translateX(2%); }
    .stack-block--3 { width: 82%; background: #f59e0b; transform: translateX(-2%); }
    .stack-block--4 { width: 72%; background: #84cc16; transform: translateX(1%); }
    .stack-block--5 { width: 61%; background: #38bdf8; transform: translateX(-4%); }
    .stack-block--moving {
        width: 52%;
        background: #8b5cf6;
        animation: block-slide 2.6s ease-in-out infinite alternate;
    }

    .stack-score {
        position: absolute;
        top: 1rem;
        left: 1rem;
        color: #fbbf24;
        font-family: var(--font-utility);
        font-size: 0.72rem;
        letter-spacing: 0.1em;
    }

    .stack-notes {
        display: flex;
        flex-direction: column;
        justify-content: center;
        padding: clamp(1.5rem, 5vw, 3.5rem);
    }

    .stack-kicker {
        margin-bottom: 0.8rem;
        color: #fbbf24;
        font-family: var(--font-utility);
        font-size: 0.7rem;
        font-weight: 600;
        letter-spacing: 0.08em;
        text-transform: uppercase;
    }

    .stack-notes h2 {
        max-width: 32rem;
        margin-bottom: 1rem;
        font-size: clamp(2rem, 5vw, 3.4rem);
        line-height: 1.02;
        letter-spacing: -0.045em;
    }

    .stack-notes > p:not(.stack-kicker) {
        max-width: 36rem;
        color: #cbd5e1;
    }

    .stack-notes dl {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        margin: 1.2rem 0 1.6rem;
        border-block: 1px solid #334155;
    }

    .stack-notes dl div { padding: 0.8rem 0.55rem 0.8rem 0; }
    .stack-notes dt { color: #64748b; font-family: var(--font-utility); font-size: 0.62rem; text-transform: uppercase; }
    .stack-notes dd { margin: 0.2rem 0 0; font-size: 0.82rem; font-weight: 700; }
    .stack-notes .button { align-self: flex-start; }

    @keyframes block-slide {
        from { transform: translateX(-44%); }
        to { transform: translateX(44%); }
    }

    @media (max-width: 760px) {
        .stack-lab { grid-template-columns: 1fr; }
        .stack-stage { min-height: 22rem; border-right: 0; border-bottom: 1px solid #4b3823; }
        .stack-notes dl { grid-template-columns: 1fr; }
        .stack-notes dl div + div { border-top: 1px solid #334155; }
    }
</style>
