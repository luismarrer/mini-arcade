<section class="tetris-build" aria-labelledby="tetris-status">
    <div class="tetris-board" aria-hidden="true">
        <div class="tetris-grid">
            {Array.from({ length: 80 }).map((_, index) => <span class={`cell cell--${index}`}></span>)}
        </div>
        <div class="piece piece--cyan"><i></i><i></i><i></i><i></i></div>
        <div class="piece piece--red"><i></i><i></i><i></i><i></i></div>
        <div class="piece piece--yellow"><i></i><i></i><i></i><i></i></div>
        <span class="tetris-board__label">BOARD TEST / 10 × 8</span>
    </div>

    <div class="tetris-console">
        <p class="tetris-kicker">Puzzle rebuild / queued</p>
        <h2 id="tetris-status">The blocks are here. The rules are being wired up.</h2>
        <p>
            This version is waiting on rotation, collision and line-clearing logic.
            Until those three feel right, it stays marked as a build instead of a game.
        </p>
        <ol>
            <li><span>01</span> Rotation system</li>
            <li><span>02</span> Line + level scoring</li>
            <li><span>03</span> Keyboard and touch controls</li>
        </ol>
        <a href="/all-games" class="button button--secondary button--small">Back to game select</a>
    </div>
</section>

<style>
    .tetris-build {
        display: grid;
        grid-template-columns: minmax(17rem, 0.82fr) minmax(0, 1.18fr);
        gap: clamp(1.2rem, 4vw, 3.5rem);
        align-items: center;
        padding: clamp(1rem, 4vw, 3rem);
        border: 1px solid #184b59;
        border-radius: 0.75rem;
        color: #dff9ff;
        background:
            linear-gradient(rgba(34, 184, 207, 0.035) 1px, transparent 1px),
            linear-gradient(90deg, rgba(34, 184, 207, 0.035) 1px, transparent 1px),
            #07151b;
        background-size: 24px 24px;
    }

    .tetris-board {
        position: relative;
        width: min(100%, 21rem);
        justify-self: center;
        padding: 1.75rem 1rem 1rem;
        border: 2px solid #22b8cf;
        background: #071117;
        box-shadow: 0 0 32px rgba(34, 184, 207, 0.18), inset 0 0 24px rgba(0, 0, 0, 0.55);
    }

    .tetris-grid {
        display: grid;
        grid-template-columns: repeat(8, 1fr);
        border: 1px solid #17343d;
    }

    .cell {
        aspect-ratio: 1;
        border: 1px solid rgba(34, 184, 207, 0.065);
    }

    .piece { position: absolute; display: grid; grid-template-columns: repeat(2, 1.25rem); }
    .piece i { aspect-ratio: 1; border: 1px solid rgba(255, 255, 255, 0.42); box-shadow: inset 0 0 0 3px rgba(255, 255, 255, 0.08); }
    .piece--cyan { right: 18%; bottom: 14%; transform: rotate(90deg); }
    .piece--cyan i { background: #22b8cf; }
    .piece--red { left: 21%; bottom: 8%; transform: rotate(-90deg); }
    .piece--red i { background: #e5454b; }
    .piece--yellow { left: 40%; bottom: 3.5%; }
    .piece--yellow i { background: #f4c430; }

    .tetris-board__label {
        position: absolute;
        top: 0.35rem;
        left: 0.55rem;
        color: #4c8794;
        font-family: var(--font-utility);
        font-size: 0.56rem;
        letter-spacing: 0.08em;
    }

    .tetris-kicker {
        margin-bottom: 0.7rem;
        color: #22b8cf;
        font-family: var(--font-utility);
        font-size: 0.68rem;
        font-weight: 600;
        letter-spacing: 0.09em;
        text-transform: uppercase;
    }

    .tetris-console h2 {
        max-width: 36rem;
        margin-bottom: 1rem;
        font-size: clamp(2rem, 5vw, 3.6rem);
        line-height: 1;
        letter-spacing: -0.045em;
    }

    .tetris-console > p:not(.tetris-kicker) { max-width: 36rem; color: #8fb2ba; }
    .tetris-console ol { margin: 1.5rem 0; padding: 0; border-block: 1px solid #17343d; list-style: none; }
    .tetris-console li { display: flex; gap: 0.9rem; padding: 0.7rem 0; color: #b9d5da; font-size: 0.82rem; }
    .tetris-console li + li { border-top: 1px solid #102b33; }
    .tetris-console li span { color: #22b8cf; font-family: var(--font-utility); font-size: 0.7rem; }

    @media (max-width: 760px) {
        .tetris-build { grid-template-columns: 1fr; }
        .tetris-board { width: min(78vw, 19rem); }
    }
</style>
